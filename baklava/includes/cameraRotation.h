#pragma once
#include "raylib.h"

/// Camera Rotation
/// 
/// THis funtion makes the game able to rotate by holding the left mouse button
/// @param camear it take Camera struct
void HandleCameraRotation(Camera& camera);
